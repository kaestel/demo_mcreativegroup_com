Util.Objects["clients"] = new function() {
	this.init = function(list) {
		var i, node;

/*
		list.nodes = u.qsa("li", list);

		for(i = 0; node = list.nodes[i]; i++) {
			node.list = list;
			node.logo = u.getIJ(u.qs("a", node), "logo");
			node.style.backgroundImage = "url(/img/logo_"+node.logo+".png)";
		}
*/
	}
}